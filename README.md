## Redux Single Page Application (SPA) Test

* * *

This is rudimentary attempt at building a single page app with Redux and React.

To run this application:

* Clone the repo.
* npm install
* npm start
